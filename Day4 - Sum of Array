class Solution {
    sum(arr) {
        // code here
        let sum=0;
        for(let i=0;i<arr.length;i++){
            sum += arr[i];
        }
        return sum;
    }
    //TC - O(N)
    //SC- O(1);    
}
