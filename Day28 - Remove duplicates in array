class Solution {

    removeDuplicate(arr) {
        // your code here
        const set = new Set(), res=[];
        for(let i=0;i<arr.length;i++){
            if(!set.has(arr[i])){
                set.add(arr[i]);
                res.push(arr[i]);
            }
            
        }
        return res;
        //tc - O(n) sc-O(n);
    }
}
